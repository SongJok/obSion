{
  "$id": "https://obsion.dev/contracts/events/payloads/action.approval_decided.v1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "approval_id": {
      "format": "uuid",
      "type": "string"
    },
    "decision": {
      "enum": [
        "APPROVED",
        "REJECTED"
      ]
    },
    "purpose": {
      "enum": [
        "EXECUTE",
        "ROLLBACK"
      ]
    },
    "reason": {
      "type": "string"
    }
  },
  "required": [
    "approval_id",
    "purpose",
    "decision",
    "reason"
  ],
  "title": "action.approval_decided payload v1",
  "type": "object"
}
