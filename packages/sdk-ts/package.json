{
  "name": "@obsion/sdk",
  "version": "0.1.0",
  "description": "Official TypeScript SDK for Obsion",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  },
  "engines": {"node": ">=22"}
}
