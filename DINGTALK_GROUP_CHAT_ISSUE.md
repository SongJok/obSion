# 钉钉机器人群聊不响应问题排查

## 当前状态

✅ **单聊正常** - 机器人在单聊中能正常接收和回复消息  
❌ **群聊不响应** - 机器人在群聊中不响应 @点仔 消息

## 问题原因

根据日志分析，有以下可能原因：

### 1. 机器人未开启群消息接收权限

钉钉机器人默认只支持单聊，需要在开放平台配置群消息权限。

**解决方案**：
1. 登录 [钉钉开放平台](https://open-dev.dingtalk.com/)
2. 找到应用"点仔"（AppKey: dingonkbr6jzpcwjbnpp）
3. 进入"机器人配置"
4. 开启"接收群消息"权限
5. 配置消息接收模式为 Stream 模式
6. 保存配置

### 2. 白名单限制

日志显示：`用户白名单 0、群白名单 0、限流 20 条/分钟/人`

这意味着没有配置白名单，应该不是限制原因，但可以检查：

**解决方案**：
1. 在开放平台的"权限管理"中
2. 检查是否有 IP 白名单或用户白名单限制
3. 如果是测试阶段，移除所有白名单限制

### 3. 机器人权限范围

开放平台可能限制了机器人的使用范围。

**解决方案**：
1. 在"权限管理"中检查"可用范围"
2. 确保测试群聊在可用范围内
3. 或设置为"全员可用"

### 4. 消息订阅配置

需要确保机器人订阅了群消息事件。

**解决方案**：
1. 在开放平台"事件订阅"中
2. 确保订阅了以下事件：
   - `chat_update_title`（群聊标题变更）
   - `org_group_chat`（群消息）
   - `robot_group_chat`（@机器人的群消息）

---

## 快速诊断命令

### 检查机器人详细配置

```bash
# 获取应用详情
dws dev app get --unified-app-id 70753b24-2409-4adf-92eb-5b0d0980b3da --format json

# 列出所有应用
dws dev app list --format json
```

### 查看实时日志

```bash
# 机器人日志文件
tail -f /private/tmp/claude-501/-Users-tuwan-work-code-obsion-openWork/c26a9aeb-32f8-4780-b287-0dfbcde6de06/tasks/b1v7ld11g.output
```

### 测试机器人

```bash
# 单聊测试（已验证可用）
在钉钉中直接私聊"点仔"，发送"你好"

# 群聊测试
在群聊中发送：@点仔 你好
```

---

## 临时解决方案

在开放平台配置修改完成前，可以使用以下方式：

### 方案 1：继续使用单聊

单聊功能完全正常，可以先在单聊中测试所有功能。

### 方案 2：手动触发（仅测试用）

如果需要演示群聊功能，可以：
1. 在群里发消息
2. 复制消息内容
3. 在单聊中发给机器人
4. 将机器人回复手动转发到群里

### 方案 3：使用你之前成功的机器人

你之前测试的 `bot_agent.py` 在群聊中工作正常，可以：

```bash
# 停止当前机器人
pkill -f "dws dev connect"

# 使用之前成功的配置
dws dev connect \
  --agent-cmd "python /Users/tuwan/work/code/obsion/obSion/bot_agent.py" \
  --unified-app-id 1bfc6ab7-2a58-483d-a5d8-499dff52d682
```

但这个 app ID 现在不在列表中，可能需要重新创建或找回。

---

## 推荐操作步骤

### 立即执行

1. **登录钉钉开放平台**
   - 网址：https://open-dev.dingtalk.com/
   - 找到应用"点仔"

2. **检查机器人配置**
   - 导航：应用开发 → 机器人配置
   - 确认：
     - ✓ 消息接收方式：Stream 模式
     - ✓ 接收群消息：已开启
     - ✓ 可用范围：全员或包含测试群

3. **检查事件订阅**
   - 导航：应用开发 → 事件订阅
   - 确认订阅了机器人群消息相关事件

4. **重启机器人**
   ```bash
   # 停止当前机器人
   pkill -f "dws dev connect"
   
   # 重新启动
   ./scripts/start_dingtalk_bot.sh
   ```

5. **测试群聊**
   - 在群里发送：@点仔 你好
   - 观察日志是否出现 `convType=2` 的消息

### 验证成功标志

日志中应该看到类似这样的输出：
```
[connect] 收到 @Joony: 你好 (convType=2 convId=xxx staffId=xxx msgId=xxx)
[connect] agent 已生成回复 (custom, 耗时 XXXms): ...
[connect] 普通消息已发送 (custom, msgId=xxx)
```

其中 `convType=2` 表示群聊消息。

---

## 常见问题

### Q: 为什么单聊可以，群聊不行？

A: 钉钉机器人的单聊和群聊是两个独立的权限，需要分别配置。很多机器人默认只开启单聊。

### Q: 需要重新发布机器人吗？

A: 如果修改了权限配置，通常需要：
1. 保存配置
2. 重启本地 `dws dev connect` 进程
3. 不需要"发布"，因为 `dws dev connect` 是开发调试模式

### Q: 日志中为什么没有群消息？

A: 如果机器人没有群消息权限，钉钉 Stream 服务器就不会推送群消息事件到你的本地进程。

---

## 下一步行动

**现在立即做**：
1. 访问 https://open-dev.dingtalk.com/
2. 登录并找到"点仔"应用
3. 开启"接收群消息"权限
4. 重启机器人进程
5. 在群里测试 @点仔

**完成后告诉我**：
- 配置修改了哪些选项
- 群聊是否开始响应
- 日志中是否出现 `convType=2`

---

## 当前机器人状态

```
进程ID: 51390, 51391
应用ID: 70753b24-2409-4adf-92eb-5b0d0980b3da
连接状态: ✅ 已连接 (sessionId: bb8b5fa5-a884-11f1-a1f7-eaaa5a2b5108)
单聊: ✅ 正常工作
群聊: ❌ 等待配置修复
```

---

**更新时间**: 2026-09-05 01:30  
**状态**: 等待开放平台配置修改
