# 实时来源测试的确定性时钟

日期：2026-09-15。范围：P1 测试可靠性，不修改来源运行逻辑。

GitHub main `3dd9dee` 的运行34941916375在 `test_live_knowledge` 的 legacy/error
场景失败。本轮本地完整并行回归也复现legacy失败：20毫秒预算被数据库初次读取耗尽，
`await_live_sources`按合同停止调度，但测试仍要求 `next_poll_at` 被提前。
使用到期时钟对同一error测试重现了相同断言失败，确认该测试把调度覆盖绑在了机器速度上。

修正仅控制此测试引用的单调时钟：先允许一轮调度，再使等待到期。保留旧扫描、未来时间、
历史无证明、供应商错误和有效租约五种场景的全部原有断言，继续检查失败原因、世代、游标、
实际扫描开始时间、租约和轮询安排。新增独立用例验证初次读取已耗尽预算时，不再调度或
改写扫描状态。没有延长生产超时、跳过测试或放宽新鲜度要求。

六项针对性检查通过；来源与错误合同组合32项通过。完整最终回归与远端CI见
[P1报告](PHASE-P1-REPORT.md)。本修正没有API、数据库或运行包迁移；回退测试提交即可。
