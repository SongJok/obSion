# IM 历史投递迁移修复验证

日期：2026-09-12。依据[ADR0129](../adr/0129-legacy-im-reconciliation-migration.md)。

远端CI运行34695599574的IM迁移往返失败。本地独立空库复现缺少对账时间标记导致的
UNKNOWN更新拒绝；补数据迁移后，又复现旧回退约束遗漏UNKNOWN。修复两处根因，
保留数据库状态保护、真实回执要求和禁止未知结果自动重发。

最终真实PostgreSQL测试1项通过（7.53秒），覆盖多次全历史升级/降级、当前迁移差异检查、
三种非SENT状态的回退拒绝、事务回滚、旧SENT记录保留、新数据修复幂等和无证明状态变更拒绝。
测试自建临时空库，结束后已删除；未操作日常业务库。新增head为b4c6d8e0f2a4，数据修复
不会发送消息；旧c9d1回退恢复其真实前置版本支持的四种状态。

Ruff和格式检查通过。此前整合2650项Python通过属于上一源码快照，不替代本次迁移的
真实PostgreSQL验证。GitHub安全扫描失败另行调查，远端完整CI尚未通过；本增量不宣称
整个M1、产品化或生产就绪。日常环境已完成真实备份克隆验证并升级，6篇授权资料可用。
