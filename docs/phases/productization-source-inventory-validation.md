# 项目来源清单验证

日期：2026-09-07。状态：本地管理面切片通过；不关闭 M2、M1 或 Phase 99。

本切片为已有来源账本增加只读对账投影：冻结 Connector 版本清单与 Workspace/仓库来源绑定清单。清单默认隐藏撤销
记录，管理员可以显式查询撤销状态；输出不包含连接端点、完整配置、凭据引用、网络出口、源码或厂商权限声明。
目录发现结果仍不会自动登记或授权仓库，来源绑定仍要求管理员显式提交已有管理 API。

验证结果：

- 来源管理 REST（SQLite）：**73 passed，73 skipped**；新增清单过滤、撤销投影、跨清单脱敏与 OpenAPI 断言。
- 契约质量门禁：与上述增量复跑 **5 passed**，错误 producer 清单已登记 `admin_access_denied`。
- Ruff、Ruff format、mypy（新增 API/Schema）：通过。
- OpenAPI 已重新生成；无数据库 schema 变化、无新迁移。

限制：测试使用合成租户和 SQLite；PostgreSQL 锁竞争、真实云效目录/源码、真实密钥管理、厂商 scopes、成员关系、
沙箱来源传输和生产部署仍未验证。清单的 `active` 仅表示本地来源撤销账本状态，不代表云效当前可读。
