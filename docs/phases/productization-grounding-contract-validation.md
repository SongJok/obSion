# 问答复核、出处与有界纠错验证

日期：2026-09-12。依据 [ADR0127](../adr/0127-align-grounding-and-citation-presentation.md)。
当前仍为 Phase 98 产品化增量，完整 M1 和自主项目能力未完成。

作者原本不得自行附加引用，复核器却要检查用户要求的出处，且复核发生在平台追加引用之前。
本次统一了两个可信契约，保留逐条事实、精确原文引句、整体支持和问题完成度检查。
上轮合同问题为何被拒绝不能从旧记录确定；本次没有把推断当成历史根因。
新的整体判断字段只有在本地完整验证复核响应后才保存，便于区分有据但未答全等情况。

正文混入内部证据编号时，系统在重新检查当前权限后至多重写一次。重写消耗同一任务预算，
仍须完整事实复核与发布前权限检查；再次泄漏编号、失权或事实不支持均不能发布。
文档本身含有技术编号的正常问题仍允许保留原文标识。此轮真实模型未触发重写分支，
不能将合成测试误写成真实自主纠错成功。

| 真实隔离用例 | 结果 | 验收边界 |
| --- | --- | --- |
| 合同编号三类 | 51.35 秒；销售、采购、投融资三类及模板边界准确，平台追加文档标题引用 | 2 次 K3 成功，完整复核通过；发生在最终正文纠错/读取改动之前 |
| 财务表格归类 | 67.93 秒；正确识别交通费用和飞机/火车子列，xx 元明确为模板占位 | 最终代码，2 次 K3 成功，完整复核通过，正文无内部编号 |
| 财务作答期间读取结果 | 状态读取 0.038 秒；含主动 0.4 秒观察等待的整个结果读取检查 0.459 秒 | HTTP 200、任务仍在运行、无观察到的 PostgreSQL 阻塞；不是延迟 SLA |

隔离模型通过正式 Model Gateway 调用，仅使用用户允许的 `kimi-k3-kimi`；配置仍只允许
K3/K2.7。来源来自已有 zziv/Joony 连接，经 Gateway、Policy 和当前权限检查获取。
本地观察器只旁路保存真实返回内容用于诊断，不改变请求、响应或校验结果；原始输出位于
忽略目录，不进入仓库报告。账本仅保留散列、调用结果及结论。

真实数据库先复现读取等待：PolicyDecision 插入被正在作答的 Run 行锁阻塞。只读检查现在
省略可选 Run 外键，仍记录资源和审计关联。最终代码的 10 项 PostgreSQL 测试覆盖五条读取
路径与正常/撤权组合：正常内容及时可读，撤权仍返回拒绝或空集合，审计仍可追溯。
临时测试数据库及失败复现数据库均已清理。无新增迁移，升级与无差异检查通过。

最终整合 Python 回归 2596 项通过、262 项显式跳过、7 项真实外部验收排除，耗时 704.80 秒。
专项 86 项通过（86.78 秒），此前正文/复核专项 45 项通过（11.65 秒）；273 份源码 Mypy、
Ruff/1044 份格式、Event/Error 合同和秘密扫描 0 项通过。GitHub main重新获取后为0/0，未提交或推送。


日常环境已备份并更新为 `obsion-api:grounding-contract-20260912`，248 份 API Python 源码
逐一匹配，文档同步宿主固定到同一验证版本。来源先暂停，恢复后重新检查期间先有 1 篇
可用，随后 6 篇全部恢复，旧文档标识保留。第 30 条在刷新期间发送，但入站时来源已恢复；
第 31 条在确认 6 篇可用后发送。没有将刷新过渡期当作文档丢失。

| 日常点仔用例 | 实际聊天与复核 | 时序及回执 |
| --- | --- | --- |
| 30：合同三类 | 销售、采购、投融资及文档出处；整体支持、问题完成、逐条证据复核通过 | 103 秒；其中进入本地处理前 60.700 秒；单次 SUCCESS/UNREAD |
| 31：费用归类 | 飞机/火车属于交通费用，xx 元占位不作为已批准标准；完整复核通过 | 80 秒；进入本地处理前 0.766 秒；单次 SUCCESS/UNREAD |

两条消息均核对同一 zziv/Joony、安装、聊天、真实用户消息 ID、Inbox、Run、Outbox 和机器人
消息 ID；实际聊天正文与已核验产物完全一致，各两次 K3 成功。没有内部证据编号，也没有
实际触发正文重写。回执不代表人工已读。旧第 27 条截至再次检查仍未见同 ID 入站，不重发，
也不将原因归给特定上游组件。全部本轮调用和时序见
[证据账本](../release/evidence/productization/20260912-grounding-contract.json)。

隔离来源再次暂停后，两条企业历史答案返回404，任务状态保留且计划隐藏；没有活动任务、
活动来源或租约。隔离 API/Worker 已停止，独立 Redis 已删除，数据库保留为暂停状态。
日常 API、文档宿主、原 Stream 和 Inbox 进程继续运行。

用户新增要求明确质量优先于速度。后续重点是有证据缺口时的多轮自主分析、受控工具/文档
调用与再次复核，所有外部能力仍通过 Gateway 和 Policy；不能通过减少事实检查换取速度，
也不能把一次正文纠错描述为完整自主循环。完整文档格式、M1/M2/M3 与自主项目仍待完成。
