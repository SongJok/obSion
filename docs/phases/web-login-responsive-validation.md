# Web 登录入口响应式验证

日期：2026-09-05

## 本地改动

- 手机介绍区压缩留白；保留表单安全说明与错误提示。
- 登录输入文字为 16px，输入框与提交按钮高 48px，方式切换按钮至少 44px。
- `.login-shell` 使用 `height: 100dvh; min-height: 0` 与内部纵向滚动。此前只有最小高度，内容可能将容器撑出禁止滚动的页面根节点，导致矮屏底部内容无法正常滚动到达。
- 未修改认证 API、身份策略或凭据处理。

## 验证范围与结果

最新本地生产构建成功；布局验证使用独立服务 `127.0.0.1:53011`，后续键盘补丁验证使用 `127.0.0.1:53012`，未重启已有服务。

通过独立 Chrome 的 CDP 执行真实浏览器验证：

1. 1440×1000、1024×768、390×844、320×740：页面无横向溢出，输入文字与控件尺寸断言通过。
2. 320×568、390×400、844×390、1024×400：真实滚轮事件触发登录容器滚动，提交按钮与底部说明均能滚动到可见范围。
3. 六次真实鼠标点击：显示／隐藏密码、切换令牌登录、显示／隐藏令牌、返回密码登录；模式及输入类型断言通过，空输入提交保持禁用。
4. 模拟 `prefers-reduced-motion: reduce`，确认设置匹配，登录提交按钮动画与过渡持续时间符合减少动态效果规则。

5. 登录方式键盘补丁：1440×1000 与 390×844 下，通过真实 CDP 按键验证 ArrowLeft/ArrowRight 循环切换、Home/End 首尾切换（含重复 Home）；选中状态、焦点与唯一 Tab 入口保持一致。Tab 进入对应邮箱／令牌输入框，Shift+Tab 返回选项卡。焦点稳定后才读取断言，不以 jsdom 结果替代真实浏览器。

自动检查：键盘补丁整合后，24 个测试文件、195 项测试通过；无临时类型补齐的独立 TypeScript 检查、ESLint 和生产构建通过。

## 证据边界

- 浏览器驱动脚本与 JSON 测量结果位于本次会话的 `/tmp`，不是已纳入仓库的正式端到端回归套件，不保证跨会话保留。
- 没有提交认证请求，没有读取或使用真实凭据。
- 视口缩小不等同于真实手机软键盘测试；没有声称通过移动设备实机验收。
- DOM 尺寸与可达性断言不等同于审美验收；登录后工作台尚未完成真实浏览器验收。
- 移动导航键盘补丁仍在隔离 worktree，整合操作被隔离边界阻止，不能计入主目录成果。
