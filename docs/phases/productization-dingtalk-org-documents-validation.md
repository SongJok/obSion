# 钉钉组织文档验证

日期：2026-09-11。状态：DISCOVERY_VERIFIED_TEST_TENANT_SYNC_IN_PROGRESS。

本增量服务于用户追加的“自动获取该钉钉组织下的所有文档信息”。依据
[ADR 0113](../adr/0113-organization-bound-dingtalk-wiki-discovery.md)，保持原企业 AI 底座目标。

真实 zziv 操作者目录取得两个组织知识库、一个空的全员文件夹。递归遍历后为三份文件夹、
10 篇 adoc、1 份 able。该结果只证明当前账号可见集合，不证明拥有组织所有私人文档的权限。
[操作者目录账本](../release/evidence/productization/20260911-dingtalk-org-discovery.json) 保留初始发现。

机器人原应用浏览返回 403。用户随后完成文档授权，实查 Wiki.Workspace.Read / Wiki.Node.Read
已生效。操作者 unionId 解析首次真实返回 60011/qyapi_get_member 未授权；用户补齐权限后，同一
应用、同一用户查询成功，没有切换身份绕过拒绝。凭据没有写入代码、报告或模型上下文。

当前源码通过真实应用凭据、隔离 PostgreSQL、实际 REST → CapabilityGateway → Policy →
CredentialBroker → Wiki v2 链路取得两个知识库及 14 个节点，三个浏览请求均有 SUCCESS 审计与
PolicyDecision ID。新版 workspaceId 与 DWS/旧版编号不同，已使用新版原始返回值。
[真实应用目录账本](../release/evidence/productization/20260911-dingtalk-wiki-v2-live.json) 保留结果。
未调用模型，未修改钉钉文档或权限。

专项及现有知识连接/REST/契约组合：50 passed、1 skipped（43.33 秒）；跳过为显式 Feishu
真实租户验收，不计通过。含 25 项新增目录/REST 检查，覆盖分页、空页游标、跨组织与私人空间
隔离、循环、预算、类型、受管执行、审计及禁止把新编号送入旧正文接口。Ruff 与新增两源文件
Mypy 通过。一次性 PostgreSQL 空库已升级至 d0e2f5a7b3c4；无新增迁移，drift 已通过。

完整同步尚未完成：现代正文读取与格式解析、来源身份/ACL 映射、持久任务与恢复、自动刷新和
撤销均仍在进行。当前新协议明确拒绝单文档导入和工作区同步，不把全部跳过伪装成成功。
本增量尚未替换运行中的开发 API；原来的实际问答镜像与 Kimi 限制保持生效。M1、企业全量
文档问答、自主项目闭环与生产晋级不声明完成。

用户补齐三项只读权限后，应用正文块接口与文件权限接口也已实测成功，已确认同组织 OWNER。
但正文块接口 success=true 时漏掉两段 blockquote，分别70与86字符。与同文档 revision 1
完整 JSONML 比较：9个块中7个文本一致，两段引用正文缺失。未把该不完整结果入库或交给模型。
[内容完整性账本](../release/evidence/productization/20260911-dingtalk-body-completeness.json)
只记录类型和长度，不包含正文或个人信息。下一步需完整正文读取渠道及明确缺失状态，不能只看HTTP成功。

独立PostgreSQL Alembic check 无差异；控制面231源文件Mypy通过。

逐篇只读检查已取得10篇文字文档的完整JSONML，以及各自的应用侧权限列表；其中3篇的
原生块响应存在缺失属性。完整结构读取来自已登录的同组织操作者，仍不等于自动后台同步。
[逐篇无正文账本](../release/evidence/productization/20260911-dingtalk-body-coverage.json) 保留类型、
数量和读取状态；AI表格正文仍未读取。持久闭环的边界见[同步契约](../product/dingtalk-source-sync-contract.md)。

一次从仓库根目录直接启动的全量检查误读取本地模型限制配置，导致5个使用MockTransport的
模型测试在调用前被allowlist拒绝；该运行已中止（1254通过、222跳过、4排除）。没有为测试
放宽实际模型限制，也没有调用其他真实模型。改用仓库既有隔离入口后，完整 Python 回归 **2407 passed、223 skipped、7 deselected，588.58 秒**。
此后复查再补四项显式业务失败响应不得伪装空目录的检查，增量组合 **54 passed、1 skipped，43.28 秒**，Ruff/格式/新增源文件Mypy再次通过。
全仓 Ruff、992文件格式、256源文件Mypy、契约/状态与秘密扫描通过。

## ADR0114 开发受管正文读取

2026-09-11 21:18:18—21:18:41 +08:00，在同一隔离 PostgreSQL 的真实 Gateway/Policy 下，
新的 `knowledge.dingtalk.read` SDK 能力读取 zziv 已发现的全部10篇 adoc，10次均 COMPLETED
且有 SUCCESS Audit。3篇解析无缺口，7篇保留图片、卡片、复杂表格或有序编号等缺口；
COMPLETED 仅表示这次读取结束。最新身份信息文档真实 revision 已从早前1变为5，
本次没有复用诊断缓存或把旧正文当作新版。

[正文读取账本](../release/evidence/productization/20260911-dingtalk-managed-reader.json)
仅包含标识、长度、摘要、版本、缺口和审计标识。执行器固定已核验组织/用户，应用原生
权限检查先于 DWS 读取，并在读取后再次检查权限、绑定、有效用户和连接；权限列表未完整
取得、只有预览、无明确USER读取角色或同名不同组织均拒绝。策略资源与真实节点必须一致。
正文中的引用块保留，元数据、链接token和嵌入属性不展平为依据。

真实读取后补充空白叶占位不应阻止 code 正文解析的修复，并验证有界进程输出、错误不泄露
及取消后子进程回收。最终新增39项、Wiki29项、SDK兼容8项、契约5项，共
**81 passed，49.24秒**；全仓Ruff及控制面233源文件Mypy通过。本增量未再次运行完整Python套件；
上面的2407项全量证据仅适用于之前冻结的目录整合，不能当作本增量的完整回归。

无新schema或业务迁移。本次只在隔离验收库登记开发能力与连接fixture；未改变运行中API、
未调用模型、未发送钉钉消息、未修改用户文档。默认API仍不安装受管DWS执行器。
**正文入库、持续同步与撤销检索屏障尚未完成**，下一步按同步契约实现持久状态和原子入库。

## ADR0115 受管入库、访问屏障与持久任务状态

迁移 e1f3a6b8c5d7 新增 knowledge_sync_sources/items 和同组织文档唯一键，复用既有连接版本
与撤销账本。来源注册、入库和关闭经过当前身份、L2 Policy 及 Audit；正文与访问记录原子
提交。只有完整正文可建立 READY 文档，不完整更新会撤销此前的访问有效期。

2026-09-11 21:42:06—21:42:30 +08:00，在隔离 PostgreSQL 实际读取全部10篇adoc：
3篇READY并能检索；7篇PARTIAL、没有可检索文档。关闭来源后3篇均无法读取。
[无正文入库账本](../release/evidence/productization/20260911-dingtalk-governed-ingest.json)
记录状态、版本、文档标识和审计结果；显式使用内存blob适配器，未验证重启后的blob读取。
未调用模型、未发送消息、未更换正在运行的开发API；该结果不代表后台自动同步或点仔问答。

本地刷新回归发现 READY 与空租约的中间自动flush冲突，已通过事务内先进入PENDING解决，
数据库约束保持。最终 SQLite/PostgreSQL **28 passed，28.03秒**，覆盖幂等租约刷新、
同正文的新来源revision形成新版本、不完整、来源/安装/绑定/凭据/配置/grants/版本撤销、
过期授权、其他管理员和无访问记录拒绝，以及有界任务继续位置、旧token拒绝与完整扫描后
缺失判定。独立 PostgreSQL 完成空库到head、降至d0e2f5a7b3c4、再升head及无差异检查。
Ruff、1003文件格式及全包261源码Mypy通过。该切片完整Python为2465passed、237skipped、7deselected，另1项失败是新增两个错误字段未登记在冻结库存；补齐库存后错误/契约/来源/读取组合77passed、14skipped（66.05秒）。这不冒充一次新的完整全绿回归，ADR0116另行整体验证。

后台发现/读取/重试循环、公开管理入口、持久blob、复杂内容、AI表格及实际点仔文档问答仍未
完成。远端撤权依赖下一次检查或最多5分钟的租约到期，不宣称已实现远端事件或零延迟撤权。

## ADR0116：真实后台扫描、断点恢复和持久文件

新增 `knowledge.dingtalk.discover` 固定只读 SDK 能力、可选的 Operator Connector pin、单一
Python 宿主 Worker。每页/每篇提交继续位置；过期/替换租约或来源关闭阻止旧结果提交。
节点发现重复游标或失败页会保留此前进度，恢复后继续；不再用一次200节点的遍历预算
充当后台全量目录。范围检查仍有显式空间分页与64层深度预算，超过预算不标记成功。

真实验证发现 DWS `profile list` 只报告 expired，不自动刷新。新宿主调用固定账号的
`auth status` 刷新，再重查同组织和用户 active；真实旧令牌刷新后完整正文读取成功。
未切换全局组织、未登录新账号、未要求用户再次开通已授权权限。

[2026-09-12真实Worker账本](../release/evidence/productization/20260912-dingtalk-worker.json)：
目录变为15节点，其中11篇adoc、3目录、1AI表格。19个调度检查中完成18个实际工作单元，
3篇READY、7篇正文PARTIAL、1篇新建adoc无文字而FAILED（document_parse_failed）；3目录及
1AI表格是显式PARTIAL。每个工作单元重建Worker；最后重建Database和MinIO客户端，3篇
文件均通过原有下载完整性校验。停用来源后3篇均拒绝访问。完整扫描状态不是全文件成功。
精确单元计数以账本 `units` 为准。

这次真实验证使用专用PostgreSQL验收库与专用MinIO桶，不使用内存文件存储。用户文档没有
被改写。默认开发API未换镜像，未发送消息或调用模型；不能把此结果写成点仔已能回答
所有企业文档。管理入口、常驻运行环境接入、复杂正文/AI表格、最终回复/历史访问再校验
仍需继续实现。

新增测试包含页间重建Worker、重复/缺失页恢复、其他组织连接同时存在时固定选择、来源
停用/租约替换后不入库、预览权限不读取正文、空正文失败、刷新后身份错配/重复/仍过期拒绝。
来源权限双数据库34passed（35.59秒）；登录刷新与Worker组合50passed（18.20秒，之后增加
空正文用例并纳入完整回归）。控制面及SDK/CLI/IM共263源码Mypy、Ruff及1007文件格式通过。


本轮完整隔离Python回归 **2481 passed、240 skipped、7 deselected，643.94秒**，
包括SDK/CLI/IM兼容性，未加载本地真实集成配置。运行时错误契约与冻结字段库存已通过。
随后复核新增两项到期边界用例，证实失败提交路径可能在最后租约检查失败后保留条目变化；
修复为整个事务回滚，涵盖单条ORM更新和批量访问撤销。新增用例先失败，修复后与Worker、
受管读取、错误及契约检查组合复测；这项小增量不冒称又跑过一遍全量。
真实周期的6次目录与11次正文读取全部有Policy关联审计，详见账本新增audit_verification。

最终失败租约增量组合 **77 passed，56.67秒**（10个Worker/指纹/事务边界用例、43个受管读取
用例、24个错误与契约检查），没有失败项。263源码Mypy、全仓Ruff、1007文件格式、阶段声明
与秘密扫描通过（0发现）。真实业务验证在这项到期边界补丁之前完成；补丁的单条/批量回滚
由失败先复现、修复后通过的测试证明，未伪造一次新的真实同步或全量回归。


## 2026-09-12 source management (ADR0117)

新增同主体来源管理REST与企业知识页面，可选择已授权连接、分页查看来源/逐篇状态、暂停、恢复和重新扫描。多连接不默认选择第一个；恢复不续期旧授权，先将旧READY进入待核验。拒绝操作回滚业务变化但保留Policy关联拒绝审计。刷新失败清空旧列表，避免继续显示过期可用数量。

真实隔离浏览器通过管理API恢复既有zziv来源；后台经Gateway/Policy只读完成新一轮扫描，页面验证3篇可用、12文件、8个不完整/不支持文件、1空正文失败，以及逐篇原因。390像素窄屏按钮和换行可用；通过页面暂停后显示0可用，随后3篇get_document全部拒绝，暂停审计关联Policy。元信息证据见[来源管理账本](../release/evidence/productization/20260912-dingtalk-source-management.json)。目录共15节点，3目录不计入文件数量。验收来源最终保持暂停。

当前增量组合 **57 passed、17显式PostgreSQL跳过，71.96秒**，覆盖管理API、来源访问、Worker和错误/契约；Web全量 **221 passed，16.53秒**，包含6个新增来源页面测试；控制面240源码Mypy及Web lint通过。浏览器使用隔离API、真实PostgreSQL来源状态和专用MinIO后台读取；预览API的默认内存blob不用于正文下载验收。复用e1f3a6b8c5d7，无新schema迁移。此增量尚未重复完整Python回归，前文2481为ADR0116结果。

普通运行API/Web没有部署本增量，未发送IM消息或调用模型。最终发布和历史访问权限复核、完整格式、常驻同步运行接入以及点仔自动文档答案仍需继续完成；不推进完整M1或生产晋级。


## 2026-09-12 managed answer access checkpoints (ADR0118)

Harness新增作答前、独立复核前和复核后的受管来源权限/版本检查，经Policy和无正文审计。已撤权内容不进入作者；作者执行中失权不再进入复核模型；复核过程中失权转WITHHOLD，不发布候选答案、引用、报告正文或answer.delta。Document再次读取强制刷新ORM，避免同一会话继续使用旧ACL。沿用既有Critic conflict及事件契约，无新增事件版本、迁移或外部调用。

专项首先发现新的checks字段不符合严格事件契约，修复为既有冲突结构记录权限变化；到期测试夹具同时保留checked_at早于expiry的数据库不变量。组合功能验证100passed/17显式PostgreSQL跳过，错误来源库存因代码行移动需更新；修复库存后最终 **33passed（58.18秒）**，包括9个受管Harness场景与错误/契约门禁，新增ACL缓存场景也通过。控制面241源码Mypy、Ruff和1014文件格式通过。Web最新221passed、lint/typecheck及生产构建通过。来源管理新增OpenAPI当前性在最终契约组合中验证。

验证使用合成受管来源与合成模型返回，未调用新的真实Kimi或发送钉钉消息；前文真实来源管理账本独立保留。历史答案/Claim/Evidence/产物/事件回放、对话与记忆复用、复制Run和钉钉最终发送仍需来源访问控制；最后检查到提交/发送间的并发撤销边界尚未关闭，须补PostgreSQL并发验证。普通运行环境不启用新来源，不宣称最终发布与历史访问权限已完整完成。详见[ADR0118](../adr/0118-managed-answer-access-checkpoints.md)。


## 2026-09-12 historical and derived source access (ADR0119)

历史 REST/AppServer、SSE每次读取、Workspace列表及产物下载改为核验同组织的Run来源图。回放源、平台产物来源与实际使用的历史对话向上继承权限；失权后旧答案及副本不返回，列表不显示敏感内容，读取拒绝仍留下Policy关联审计。任务状态可读，但计划、意图和压缩上下文等内容被移除；Web不会继续显示缓存的失权答案或复制/回放入口。未使用受管历史的日常问答保持可用。

Outbox排队和发送前检查可信安装的corpId及收件主体当前资料权限，暂停来源或改成其他组织都会阻止实际Gateway调用。旧IM兼容最终回复也核验来源，缺少可信组织时不能发送受管内容。没有放宽原有群受众与回执规则。

14项合成Harness场景通过（39.27秒）；相关历史、任务、产物、对话与IM组合先后88项和28项通过。Web全量222项通过（13.79秒），lint、生产构建和构建后typecheck通过；控制面244源码Mypy、全仓Ruff和1018文件格式通过。错误来源库存按新增拒绝和现有转发位置更新；完整隔离Python回归及最终补丁结果如下。

[PostgreSQL元信息账本](../release/evidence/productization/20260912-managed-history-postgres.json)记录五项真实数据库验证：历史撤权、回放副本、排队后撤权、错误发送组织、未使用企业历史的日常问答。首次回放触发既有复合外键失败；修复父组显式flush顺序后，五项全部通过。每个场景从已迁移到e1f3a6b8c5d7的模板建立独立数据库，成功库和此前失败诊断库均已删除。这是实数据库/合成模型与非发送Gateway验证，不是真实模型或钉钉消息验收。

无新schema迁移，正常API/Web未部署。来源图超过1000个Run或10000条证据即拒绝内容，不把截断当作完整。最后检查与提交/网络发送仍有并发窗口，大图性能和人工记忆来源策略需继续完善；不把顺序场景当作PostgreSQL并发撤权验收。常驻正常环境、复杂文档/AI表格和真实点仔自动资料答案仍未通过，完整M1与生产门禁保持未完成。


本轮完整隔离Python回归为 **2502 passed、240 skipped、7 deselected、1 failed，820.64秒**。唯一失败是旧故障分析黄金样例直接构造未初始化的Harness，缺少新增权限组件；夹具补入真实KnowledgePublicationGuard，原断言保留。没有通过移除权限检查、删除断言或跳过用例来消除失败。

额外的PostgreSQL失败先复现了AppServer幂等操作缓存绕过来源撤权的问题。修复在返回原操作结果前重新检查Workspace与来源权限；有效时保留原始响应，失权时移除敏感Run字段；不重复执行操作，不修改不可变请求账本。测试同时验证只有一条请求记录、记录仍为原结果，以及暂停后重试无法取回原意图/计划。首次补丁重新转换了已公开的意图，导致格式不一致，随后修复为有效响应原样返回。

最终包括14项来源Harness场景、旧故障分析黄金样例、AppServer生命周期/协议/分层、错误库存与事件契约的组合 **68 passed，87.55秒**；最后代码版本的五项PostgreSQL场景也全部重验通过，所有本轮诊断库已删除。全包控制面/SDK/CLI/IM **269源码Mypy**、全仓Ruff和**1019文件格式**通过。Web沿用本轮222项、lint、构建和typecheck结果，其后没有前端改动。该68项复测不是又一次完整Python回归；全量结果和补丁结果分别记录。


## 2026-09-12 local publication and revocation ordering (ADR0120)

最终Harness发布及新版DingTalk Outbox在当前权限读取前取得同组织共享事务屏障，来源与权限事实的23表写入经数据库触发器取得排他屏障。作者/复核模型阶段不持有屏障；发布和发送持有到事务结束。更新先提交时重新读取最新权限，发布先执行时撤权等待其事务结束。同组织发布可并行，其他组织写入不被阻塞。来源屏障等待超过2秒或非READ COMMITTED事务会拒绝继续；Outbox在未调用Gateway时保存BLOCKED，可重新核验同一逻辑投递。

隔离PostgreSQL **10 passed，9.37秒**，通过真实pg_blocking_pids观察等待，覆盖提交/回滚、反向先后次序、并发读、跨组织、等待超时、拒绝旧快照、真实Harness发布、真实Outbox事务和JSON序列化差异。测试模型与消息结果为显式合成适配器，没有真实模型调用或IM发送。首次跨组织测试复用了相同虚构厂商应用标识，触发全局唯一约束；测试改为独立应用标识，约束保持。连接JSONB等价但序列化顺序变化的绕过先失败复现，随后触发器增加原始JSON文本比较，测试通过。

f2a4b6c8d0e1从空库升级、降至e1f3a6b8c5d7、再次升级和Alembic无差异均通过。源表与现有数据没有被删除或重写。5个既有历史/副本/组织投递PostgreSQL场景也在新迁移上通过。来源/Worker/正文读取/回放/Outbox/错误事件契约 **118 passed，89.99秒**；新增错误码的精确目录由343至344、活跃来源由341至342，保持全量来源集合相等断言。该增量没有重复完整Python回归，前文全量结果独立保留。

为限制持有屏障的网络阶段，单聊与群聊增加30秒总发送时限，覆盖令牌获取和一次消息请求，保留发送前NOT_ATTEMPTED、发送后UNKNOWN及不盲重试语义。最终总时限/原生发送/Outbox/错误与事件契约组合 **119 passed，41.56秒**；其中4个新增慢令牌/慢消息、单聊/群聊测试证明总时限及NOT_ATTEMPTED/UNKNOWN边界。当前270源码Mypy、Ruff及1023文件格式、差异空白检查通过。主分支已重新fetch，HEAD与origin/main提交差异0/0；工作树改动仍未提交，普通环境未部署。

未部署普通运行API/Web、未迁移原真实来源验证库。远端权限仍受既有租约约束；厂商POST前时钟新鲜度、旧兼容IM跨进程明文发送边界、其他授权/谱系变化面及正常环境接入仍需继续处理。不把本地事务顺序当成外部消息可撤回，不宣称完整M1或全企业文档覆盖。详见[ADR0120](../adr/0120-source-publication-serialization.md)。

无正文账本见[发布并发与时限验证](../release/evidence/productization/20260912-source-publication-fence.json)。本轮所有失败诊断库和成功临时库均已删除。


## 2026-09-12 最后发送检查与群成员资料权限（ADR0121）

2026-09-12 ADR0121：单聊/群聊取得令牌后、消息POST前重查租约、资料、组织及群受众；群答案对每个成员核验来源，无权成员存在时仅发固定状态。旧IM明文接口拒绝受管正文，业务回滚后另事务保留Policy关联拒绝审计。最终167项通过（110.97秒）、9项隔离PostgreSQL场景通过；270源码Mypy、Ruff/1025格式与秘密扫描通过。复用f2a4b6c8d0e1，无新迁移或普通环境部署；真实点仔文档问答和完整M1继续验收。

失败先复现了工作区成员缺少受管来源权限仍收到引用正文的问题。修复为发送正文前逐成员检查来源和Policy，保留已有固定状态降级；最终发送回调再次核验状态与内容指纹。单聊/群聊源租约和发送租约在取令牌期间到期、群受众过期均不提交消息POST；有效授权仍能提交。旧接口以有效来源权限为前提验证拒绝交出正文、没有投递记录、拒绝审计持久存在。

本轮群测试初始夹具还存在参数默认值、Run没有workspace_id以及成员缺少created_at三处构造错误，均修正为真实约束要求，没有移除约束或断言。最后组合167项、PostgreSQL九个独立数据库场景全部通过；成功库及此前失败的两个诊断库均已删除并确认无残留。真实Harness/Gateway/Policy/凭据代理运行，模型和HTTP为显式合成边界，真实模型调用与钉钉发送均为零。该结果不替代前文完整Python回归或现场问答验收。

新增原生回调覆盖严格布尔批准、单聊/群聊调用顺序以及回调超时；旧接口兼容收窄由[ADR0121](../adr/0121-final-dingtalk-send-authorization.md)批准。无新错误码，精确错误来源清单仅更新八处已审核的转发行位置。无新schema迁移，沿用当前事务屏障；普通运行API/Web及真实来源验证数据库没有部署或迁移。无正文证据见[最后发送验证账本](../release/evidence/productization/20260912-final-dingtalk-send.json)。


## 2026-09-12 持续同步下的真实问答与进度读取（ADR0122/0123）

2026-09-12 ADR0122/0123：真实zziv持续同步+Kimi问答发现并修复例行扫描清除有效授权、状态审计等待模型Run锁两处问题。新增read_generation及a3b5c7d9e1f2迁移，6项同步PostgreSQL和12项并发/状态通过；最终79项回归（105.44秒）、270源码Mypy/Ruff1029通过。真实7次K3调用：日常翻译通过，两次知识答案及逐条原文复核通过，第二次持续状态可读且约44秒完成；未知数字未编造但弃答笼统，未计质量验收。暂停后企业历史拒绝、日常历史保留；隔离服务已停，普通环境未部署。此前因状态修复停止的全量不计通过；最终ADR0122/0123完整Python2532passed/252skipped/7deselected（678.51秒），该源码快照早于ADR0124；完整M1、点仔文档投递与格式覆盖继续推进。

在原验证库的独立副本中运行真实开发模式API、官方宿主同步入口、PostgreSQL、MinIO及独立Redis。指定zziv:Joony身份刷新成功，原组织默认未改变。首次请求因隔离环境未配置限流服务而在模型调用前失败，补齐独立Redis后继续；第二次请求在作者成功后被后台目录扫描错误清除授权所拦截，保留失败记录，未发布候选正文。

新增独立读取代次，使例行发现保留原来的有效期、到期仍拒绝，成功正文/权限读取才能续期；重建Worker跨两个扫描周期及过期、拒绝、不完整、暂停、缺失六项通过。空库及含真实测试文档的迁移往返验证状态、文档编号、检查时间及授权时间不变；初次ORM默认值不一致由schema检查发现并修正，约束与断言保留。

真实问题询问欢迎文档明确支持创建的四类内容，两次正确回答文件夹、文档、表格、脑图，唯一引用该受管文档的本地版本1，真实K3作者和K3原文复核均成功。第一次运行43.275秒，一次25秒状态轮询超时；通过真实pg_blocking_pids定位到元信息权限审计的Policy-to-Run外键写入等待模型行锁。保留Policy的Run资源标识、审计关联及完整权限检查，状态投影不再写可选的直接Run外键。两个先失败后通过的数据库测试包含有效授权与撤权后的敏感字段隐藏。最后一轮实际问答44.63秒，从第1秒持续显示RUNNING并最终通过四条原文复核，没有状态观察超时。它仍未满足30秒性能目标。

日常翻译约13.57秒完成，无企业引用。存储容量上限未知问题未生成数字，但落入question_not_covered的通用WITHHOLD措辞，尚不是合格的精确弃答体验。全部6个新Run共7次真实模型调用，均为kimi-k3-kimi成功；未调用其他模型、未发送钉钉消息。该记录不能替代点仔入站及回执验收。

受管来源已运行到第13代，最终3篇可读，8篇不完整/不支持、1篇空正文失败、3个目录。暂停后知识答案历史访问404，日常翻译历史仍200，敏感元信息隐藏且状态查询约0.051秒。隔离API和宿主Worker已终止，专用Redis已删除；保留暂停的obsion_managed_qa_20260912数据库以复现及后续验证，其余本轮失败诊断库均删除。普通环境与原来源验证库未升级。

本地33/17跳过、97/17跳过和最终79项组合范围重叠；6项来源PostgreSQL及最终12项并发/状态测试、迁移往返/无差异通过。完整Python回归中途因发现状态锁问题而停止，不计作成功，最终代码的新完整回归另行记录。证据及源码哈希见[真实问答账本](../release/evidence/productization/20260912-managed-source-qa.json)。


2026-09-12 ADR0124：企业问答区分资料不足、生成格式错误和模型不可用；缺项主题仅允许摘取当前用户问题的原文，最终提示由本地生成，不发布模型弃答正文。真实zziv资料与一次K3调用约24秒返回明确的容量上限缺项说明；仍为WITHHOLD/未核验、无事实引用，不冒充独立复核。暂停后该历史答案拒绝且计划隐藏，日常历史保留。最终151项通过（99.47秒）、6项隔离PostgreSQL通过，271源码Mypy、Ruff/1031格式、秘密扫描通过；无新迁移，隔离服务已停止。前版完整2532项通过与本次增量分开记账；普通环境、点仔文档投递、完整格式和M1继续推进。 详见[ADR0124](../adr/0124-controlled-insufficient-evidence-replies.md)与[验证账本](../release/evidence/productization/20260912-controlled-abstention.json)。


2026-09-12 ADR0125：补齐普通启动的受管目录/正文能力注册，测试改用正式描述符，并验证管理员接口完整注册流程。整合Python2548passed/252skipped/7deselected（633.88秒），注册专项56项、最终HTTP2项及隔离PostgreSQL2项通过；271源码Mypy、Ruff/1033格式通过。已备份、真实备份恢复试升级并将日常API/Web升级到a3b5c7d9e1f2，修复本机5432端口冲突，项目宿主端口改为56324；246后端源码与运行镜像一致。zziv/Joony来源经正式API注册并由受监督宿主进程持续同步，3篇可用、7篇adoc不完整、1篇AI表格不支持、1篇空正文失败。点仔24/25/26均真实入站并单次成功投递，聊天正文与产物一致：四类内容逐项原文复核、容量问题明确弃答、同私聊正常英文翻译；共4次K3调用。三条回执UNREAD，不声称已读；两条上游入站约60秒延迟，完整格式、时延与M1/自主项目仍继续推进。 详见[ADR0125](../adr/0125-register-managed-source-capabilities.md)与[日常环境验收账本](../release/evidence/productization/20260912-normal-managed-source-qa.json)。


2026-09-12 ADR0126：保留编号、合并表格、分栏和重复代码，检索不拆开结构片段；最终Python2579passed/252skipped/7deselected（636.10秒）、85项专项及PostgreSQL版本/撤权/恢复通过。日常API247源码与固定宿主版本一致，授权可用文档3→6，原文档标识保留。5次真实K3调用：表格含义经独立复核通过，但答案出现内部证据编号；点仔额度问题明确弃答并成功投递；合同分类三个Claim均有据却被整体复核拦截，不计问答通过。另有一条已发送消息未见入站，一条约60秒发生在本地回调前；完整质量、格式、M1和自主项目仍在推进。 详见[结构与问答验证](../phases/productization-dingtalk-layout-validation.md)与[ADR0126](../adr/0126-preserve-dingtalk-document-layout.md)。
