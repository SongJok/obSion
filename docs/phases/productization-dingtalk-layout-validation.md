# 钉钉文档结构与问答验证

日期：2026-09-12。当前为产品化 M1 文档质量增量，完整 M1 仍在进行。

依据 [ADR0126](../adr/0126-preserve-dingtalk-document-layout.md)，解析器保留经过校验的编号、
合并表格、分栏和代码正文。表格完整性需要覆盖每个物理位置；占位单元格包含正文、重叠、
越界或坐标含糊均保持不完整。检索将合并表格作为完整片段，不把表格尾部复制为独立证据。
单个结构片段超过 1,400 字符时保留明确缺口，等待大表格方案，不截断后宣称完整。

真实资料来自相同 zziv/Joony 连接，所有读取经过 Capability Gateway、Policy 和前后权限检查。
四份原始响应保存在本地忽略目录，报告仅记录校验摘要和散列，详见
[证据账本](../release/evidence/productization/20260912-dingtalk-layout.json)。

| 真实文档 | 原解析缺口 | 本次本地完整性 |
| --- | --- | --- |
| 合同管理制度 | 有序编号 | 完整，三项类别编号逐条核对 |
| 薪酬福利制度 | 有序编号；另识别出分栏结构 | 完整，分栏独立保存 |
| 财务报销制度 | 有序编号、隐藏及合并单元格；另识别出分栏结构 | 完整，5 行表格和 3 栏职责逐项核对 |
| 第一步：开始你的创作 | 编号、合并单元格、重复代码、图片/卡片/非文本元素 | 仍不完整，未发布未知图片、卡片或提及内容 |

“完整”在这里指可解释的正文和结构，并不等于内容已由企业批准。财务模板内存在“xx元”，
合同模板内存在“添加具体内容”和“xxx”；这些不能被模型改写成具体额度、联系人或已落实制度。

本地先复现 7 项失败，再验证精确编号、合并坐标、转义、列和表头角色、代码重复/冲突、
有界扫描、结构完整的检索片段和异常缺口。真实 PostgreSQL 的合成传输集成验证覆盖
正文版本替换、错误格式撤销旧正文访问、修复后同一文档标识恢复。临时数据库已删除；
现有迁移升级与无差异检查通过，无新增 schema 迁移。

日常服务在开发前已固定到上一版代码副本，开发文件不会因进程自动重启而被加载。
最终全量回归通过 2579 项，252 项显式跳过、7 项真实外部验收排除，耗时 636.10 秒；最终专项85项通过（56.20秒）、272份Python源码严格类型检查、1039份格式检查、秘密扫描0项通过。前两次中止的全量不计通过。GitHub main重新fetch为0/0，未提交或推送。

日常数据库已备份，来源先暂停，API247份源码与新镜像逐一一致后更新固定宿主快照，再恢复来源。当前可用数6，包含新增三份制度；原有三篇的文档标识保留，来源版本携带v2解析器信息。无新增迁移或生产升级。


## 真实问答结果与未通过项

| 用例 | 实际结果 | 验收边界 |
| --- | --- | --- |
| 日常 REST 表格归类 | 48.54秒；正确识别“飞机等级/火车等级”属于“交通费用”，两次K3调用和精确引文复核通过 | 正文含内部Evidence编号和body_index，事实支持通过，呈现质量未通过 |
| 点仔27：表格归类 | 同一zziv私聊已确认发送 | 截至账本记录未见同ID入站，没有重复发送，不计问答成功 |
| 点仔28：一等座具体额度 | 原文“xx元”未被编成金额；18秒实际回复明确无法确认；1次K3 | 同ID入站、产物与聊天正文一致、一次投递SUCCESS/UNREAD；不声称人工已读 |
| 点仔29：三个合同类别 | 同ID入站，两次K3；三个Claim均SUPPORTED，整体复核仍拒绝，机器人回复无法确认 | 124秒后投递成功，但知识问答未通过。当前记录未保留两个整体复核布尔值，不能臆测哪项导致拒绝 |

仅调用kimi-k3-kimi，共5次，均由模型网关记录SUCCESS。模型调用成功与答案验收分开统计。

实际Stream运行时添加本地接收时间观察器，保留原SDK、组织核对、入库请求及ACK流程；仅记录消息ID散列和时点。28号从处理入口到ACK约80毫秒；29号约86毫秒。29号的约59.8秒延迟发生在本地处理入口之前，具体上游原因未查明。原Stream进程替换为本地观察进程，工作脚本保存在忽略的宿主服务目录；这不是生产级无人值守验收。

29号运行期间读取产物列表曾在20秒超时；完成后同一接口正常返回。尚未复现其锁等待原因，不把它当作来源解析故障。下一步优先补齐整体复核诊断、答案呈现和受证据约束的纠错流程，并验证产物读取延迟。图片、卡片、提及、AI表格和大结构片段继续保留缺口。
