# Phase P1：问答根因与可信验收基线

状态：IN_PROGRESS
日期：2026-09-13

## 2026-09-15 独立评分并发撤权修复

[ADR0139](../adr/0139-independent-score-publication-fence.md)补齐评分最后检查到事务提交的
权限排序。真实一次性PostgreSQL先复现4通过、1失败：另一个事务在最终检查后成功撤权，
原评分仍准备提交。现在模型完成后才取得既有发布锁，最终权限检查与审计提交一起受保护。
修复后5项真实并发检查通过（13.33秒），包括ACL、密级、用户与Policy撤权，以及通过
`pg_blocking_pids`验证锁等待和后续重评拒绝；93项相关回归通过（58.50秒）。
没有新schema，迁移与临时容器清理成功，没有连接生产数据库。
本增量的局部回归不冒充下方2968项完整回归的新源码证明。

提交b23431b的24条完整冻结问答集另在固定开发镜像运行；本地修复没有混入该候选。
保留每个阻塞和失败样例，P1/Phase99继续未完成。

## 2026-09-15 重新启动完整产品开发与独立评分

用户重新启动完整产品目标。已逐段核对原始蓝图，并以
[完整产品需求总纲](../product/blueprint-product-contract.md)明确第四部分职责分层、
第五部分持久模型及P1—P5依赖，保留用户云效只读与允许不执行源码的明确范围决策。
下方此前“停止本次目标”的记录是上一轮收尾，不适用于本次继续开发。

[ADR0138](../adr/0138-independent-published-answer-scoring.md)实现独立评分：正常任务结束后，
管理员评测接口重新检查任务、问题、答案摘要、完整原文和权限，再通过Model Gateway
评价全部冻结规则。候选的Claims/VERIFIED/内部复核不进入评分输入；模型判断必须满足
严格结构、全部规则覆盖及逐字引用检查。超时、撤权、内容或密级变化、模型失败不能通过。
调用标识、Policy和不可变审计关联评分，候选Run的状态、预算及答案保持原值。

CLI新增可选评分模型配置，必须从冻结模型目录解析；默认未评分行为保留。缺少配置也
生成BLOCKED报告，不创建候选任务。报告增加评分器与政策摘要；旧分数不用于恢复。
无数据库结构迁移，复用原nullable ModelCall.run_id与审计，OpenAPI已同步。

验证结果：

- 初轮完整回归2965通过、2失败、280跳过；失败是来源测试20毫秒计时假设与新增错误
  构造点的审查登记。已保持原断言修正，并新增预算耗尽不再调度的测试；相关32项通过，
  其中六项确定性时间检查通过。详见[时钟验证](productization-source-clock-validation.md)。
- 最终完整回归 **2968 passed、280 skipped，461.50秒**；覆盖率 **79.68%**，原70%门槛保留。
  611个源码/测试/配置文件摘要在运行期间一致；安装包契约另有1项通过。
- 独立评分/任务入口/恢复/网关/原有引用核验136项组合通过；最终回归还包含后加的密级
  变化检查。新增/旧权限、原文、输入、超时及失败审计均经过正常HTTP链路验证。
- 一次性PostgreSQL完成升级并通过2项关联/独立模型回退账本检查，验证空Run外键及全部
  调用标识持久化；容器已清理。这不是完整数据库并发撤权或厂商UAT验收。
- 287源码类型检查、全仓Ruff、1108文件格式和秘密扫描通过；无新依赖。前端源码未改，
  本次未重复前端测试，不引用历史结果冒充本次验证。

两次真实开发验证：先部署ADR0137，原失败案例经一次片段修正后通过原grounding屏障，
给出1条引用，耗时139.02秒；此样例仍超过120秒任务要求。随后部署独立评分版本，源码
逐文件核对262项一致，备份数据库和11个资料对象并保留15条来源关联。
预先冻结的首个真实案例完成本轮钉钉采集，**70.98秒**生成带1条引用的答案，独立模型
评分再用**30.51秒、1次模型调用**返回PASS。原文及业务答案仅保存在私有验证目录。
普通Dockerfile与内置构建前端的开发镜像均构建成功；实际部署版本已逐文件核验。

该结果证明一个真实案例的端到端能力，**不证明评分器已经校准或总体正确率达标**。
完整保留集、执行版本证明、全部真实权限并发用例、性能分布和Phase99门禁仍未完成。
云效保持只读，没有发送钉钉消息或执行生产部署。
完整证据见[本轮验证账本](../release/evidence/productization/20260915-independent-answer-scoring.json)。

## 2026-09-15 服务器原文片段修正

[ADR0137](../adr/0137-server-owned-review-passages.md) 根据上一轮真实失败补齐片段选择：
首次全正面复核出现引文不匹配时，唯一一次修正请求使用同一候选和来源的连续原文片段。
模型选择标识，服务器恢复原文，再执行原精确匹配、声明覆盖和整体完成校验。
需要脱敏的正文在切分前排除；未知或跨来源标识拒绝，语义否定和原发布屏障不变。

本增量相关129项通过（59.31秒），含正常HTTP入口片段修正、持久验收、引用及流式一致性。
284源码类型、Ruff/1101文件格式、契约和秘密扫描通过。新增测试初轮误用引文`end`字段，改为已有`start + length`
合同后通过，保留还原原文断言。开发部署、真实修正及当前增量全量仍待完成；
下节2922项完整结果只属于19dd769，不能冒充本次新增源码全量通过。

本地运行镜像构建通过，259个运行包源码文件与工作区逐一匹配；尚未部署。
GitHub运行34939722344最终失败，新增安全注释定位到Java SDK Maven测试。综合质量任务
未显式配置SDK要求的JDK21，现补齐与独立Java任务一致的Temurin21设置；不跳过测试，
不降低覆盖率。失败断言详情仍未取得，不把配置差异称为已经证实并修复的唯一原因。
Java SDK与失败注释4项本地验证通过（2.27秒），远端修复结果仍待下一次CI。

依用户最新要求，本次提交同步GitHub main后停止修改并结束本次开发目标，不继续部署或
真实问答试跑。项目P1、正式Phase99和产品就绪门禁仍未完成；这次收尾不代表产品验收通过。
详见[本次最终验证账本](../release/evidence/productization/20260915-passage-repair-validation.json)。

## 2026-09-15 复核根因诊断与有界引用修正

[ADR0136](../adr/0136-actionable-review-diagnostics.md) 为同一独立复核器补充固定诊断，
保留原始严格校验和发布规则。诊断版已部署开发环境；新的真实提问完成外部重新采集后，
明确得到 `quote_not_exact`（112.33秒，复核37.224秒），答案与引用继续被拦截。
该结果将根因缩小到复核引文不匹配，而非来源未取得或未知格式故障；原始模型响应不保留。

针对该故障增加一次有界引用修正：仅第一次整体与逐条都声称支持、且引文未匹配原文时触发。
原候选与证据保持固定，补充服务端逐字引用要求，禁止拼接、转述或规范化；有效否定、资料不足、
其他结构错误和截断不重试。逐次使用剩余预算，保留调用指纹、失败阶段与费用，修正前后检查
已知取消/截止状态。第二次仍失败即停止，后续来源、Critic与发布检查完整保留。

最终相关验证 **121 passed/53.65秒**，期间源码/测试/CI配置摘要不变；283源码类型、
Ruff/1098文件格式、契约与秘密扫描通过。初轮深层JSON夹具错误假设了测试解释器递归阈值，
改为直接验证解析器RecursionError合同后通过；没有削弱原拒绝断言。
提交19dd769完整回归2922通过、279条件跳过（511.37秒），覆盖率79.65%（25284/31743行）；
安装包独立1项通过（0.44秒）。运行期间取得1489个跟踪文件快照，完成后摘要一致。
日常开发部署后的真实任务完成本轮外部采集，触发两次复核，均失败为quote_not_exact；
总计234.81秒、3次成功模型调用，答案未验证、引用0条。程序比较确认7段正文经模型网关
脱敏后均未改变，排除本样例脱敏差异；逐字复制提示未解决故障，不能记作真实问答通过。

main `e1d0285` 的CI运行34937186351仍在完整覆盖率测试步骤失败，迁移通过，容器构建跳过。
新增失败测试名称/类别的有界检查注释，便于自动定位；断言正文留在原附件，不降低测试或覆盖率门槛。
见 [复核修正账本](../release/evidence/productization/20260915-review-repair-validation.json)
与 [诊断运维说明](../operators/answer-review-diagnostics.md)。P1、Phase99及独立语义评分未完成。

## 2026-09-15 日常实时采集部署与正文可用性修复

已将已验证源码部署到日常**开发环境**：先保留数据库与11个资料对象备份，再完成
`b4c6d8e0f2a4 → c5d7e9f1a3b5` 迁移和无差异检查，依次切换固定源码的同步服务与 API。
15条来源项的文档关联保留；`knowledge.search`/`document.read` 新增120秒第4版本，旧版保留。
两个已授权模型端点的处理范围不变，没有生产操作、钉钉消息发送或云效代码运行。

首次正常提问暴露 DWS 登录失效：目录扫描完成却没有可用正文，Run 被标记 READY，
调用7次模型后约175秒返回资料不足。未发布旧正文或虚构答案，但恢复提示不准确。
[ADR0135](../adr/0135-unavailable-source-bodies.md) 增加 `source_content_unavailable`：
存在不完整文档且没有本轮有效正文时直接 BLOCKED，复用来源恢复提示并跳过生成模型。
混合成功与空目录保持原合同，没有降低逐文权限或发布要求，无新增数据库迁移。

本增量专项 **26 passed/25.26秒**，知识回答与来源相关 **56 passed、17项PostgreSQL条件跳过/
59.90秒**；独立一次性 PostgreSQL 的并发、无正文跳过模型、重新读取与迁移往返
**4 passed/21.45秒**。共86项通过，Ruff、1093文件格式、283源码类型、契约和秘密扫描通过。
未将上一轮完整覆盖率结果重复记为本增量全量回归。

用户完成原账号重新登录后恢复6份完整正文。修复后正常任务完成本轮外部扫描，7条检索
片段均来自受管钉钉；扫描窗口17次来源能力调用均关联 Policy 决策，仍有6份非目录文档
不完整。最终独立复核为 `grounding_review_invalid`，发布屏障拦截答案，引用数为0。
**真实采集恢复不等于问答验收通过**；复核原因细分、真实成功答案与独立保留集评分仍待解决。

main `d348d87` 的CI运行34934594128：迁移通过，完整覆盖率测试步骤失败，容器构建跳过；
目前公开接口不能取得失败摘要，已请求日志。同提交仅含Git跟踪文件、独立依赖环境的
macOS完整回归 **2896 passed、278 skipped/438.57秒**，覆盖率 **79.67%**，未复现远端失败；
这是旧提交的差异诊断，不是本增量全量通过，也不能替代Ubuntu CI结果。
P1保持IN_PROGRESS，Phase99继续阻断。证据见
[部署与实测账本](../release/evidence/productization/20260915-live-deployment-validation.json)。

## 2026-09-15 完整覆盖率与 CI 完成性

main `84973ec` 的 GitHub 运行34851557547也在45分钟上限被取消，不能当作完整质量通过。
已按 [ADR0134](../adr/0134-parallel-coverage-validation.md) 将覆盖率测试交给四个隔离进程，
按文件分配，保留全部目录、合并后的70%覆盖率门槛和原有容器依赖；增加逐项进度、慢测试、
超时调用栈及 JUnit 报告。仅新增锁定开发依赖 pytest-xdist/execnet，无运行代码或数据库迁移。

本地完整并行覆盖率运行 **2896 passed、278 skipped**（513.60秒），合并覆盖率 **79.67%**
（25240/31682行）；包含上一轮已修复的错误契约和恢复演练。安装包契约在独立进程中另有
**1 passed**（0.50秒），合计2897项通过。实际收集3174项，跳过项包括需显式真实环境的用例，
不能作为厂商或PostgreSQL新验收。新增门禁防止误删目录、降低阈值或吞掉失败。

辅助串行诊断在并行完整通过后主动停止，1265 passed、270 skipped/946.75秒不是完整通过，
也不用于跨环境速度倍数宣传。首次独立安装包命令未加载覆盖率插件，参数检查失败后修正调用，
正式独立测试通过；没有更改其断言。

Ruff、283源码类型检查、秘密扫描通过。完整Python锁定依赖使用2026-09-15官方漏洞库扫描，
HIGH/CRITICAL零发现；默认镜像连接超时后，通过官方GHCR取得并验证数据库层摘要。
新 main CI、日常部署、厂商UAT和独立语义评分继续按各自实际状态记账。
见 [本轮覆盖率账本](../release/evidence/productization/20260915-parallel-coverage-validation.json)。

## 2026-09-14 每轮受管知识重新采集增量

[ADR0133](../adr/0133-demand-fresh-enterprise-knowledge.md) 根据用户实时查证要求，
将企业知识默认改为等待提问后的外部扫描。实际访问仍由既有同步服务经 Gateway/Policy
完成，Run 的后续搜索只接受新扫描的完整、当前授权正文；没有来源时不把旧索引交给模型。
用户显式选定原生上传资料的阅读入口保留。大型库、云效/日志和完整多源查证仍待后续验收。

迁移 `c5d7e9f1a3b5` 保存实际扫描开始时间及完成世代，旧记录不补造证明。真实一次性
PostgreSQL 的重新读取、Run/Worker 并发和迁移往返/结构核对 **3 passed**（18.80秒）；
既有受管/原生来源、历史读取及发布屏障并发 **28 passed**（60.17秒）。外部响应均为
显式合成适配器，真实厂商 UAT 和日常部署尚未执行。

初轮迁移测试误用了不存在的测试查询字段，修正后原保留性断言通过。扩大组合初轮
149 passed、1 failed，失败来自把 PostgreSQL 双事务等待合同放到 SQLite 单写入夹具；
并发断言转由真实 PostgreSQL 正常任务入口执行并通过，未降低权限、引用或正文断言。
本轮完整 Python 回归为 **2894 passed、2 failed、271 skipped、7 deselected**（892.62秒），
605个源码/测试/配置文件在测试期间摘要不变。两项失败分别是新增代码导致的错误转发行号
登记未对齐，以及离线恢复演练没有显式选择合成索引模式；均已修复，原断言保留。
修复后覆盖实时知识、恢复演练、错误契约、配置和CI门禁的 **74 passed**（74.94秒），
期间最终源码摘要不变。两次快照仅演练源配置和错误登记两个文件不同；没有重复声称
修正后的全量通过，完整覆盖率复验交由新的CI运行确认。严格类型检查283源码、
Ruff/1091文件格式、契约与项目状态检查通过，秘密扫描零发现。客户端本轮没有改动，
未重跑客户端测试。证据见 [本轮账本](../release/evidence/productization/20260914-live-knowledge-validation.json)。

GitHub 基线 `0371712` 的质量任务达到25分钟执行上限，被取消；不记作通过。
已将同一任务时限改为45分钟，保留所有测试、覆盖率、漏洞扫描及客户端构建检查。
本轮运行前置和回退说明见 [运维说明](../operators/live-enterprise-knowledge.md)。

需求：[P1—P5 原始要求](../product/productization-p1-p5-requirements.md)。
架构门：[P1 gate](../architecture/phase-p1-context-quality-gate.md)。
决策：[ADR0131](../adr/0131-p1-context-and-honest-quality-baseline.md)。

## 2026-09-14 可恢复验收增量

[ADR0132](../adr/0132-recoverable-product-acceptance.md) 将创建前状态与截止时间持久到
验收检查点。新增 `--resume-from` 续查已知任务并生成独立报告；恢复重新核对当前任务
归属、问题、权限、来源、配置与实际答案。旧分数不能复用，预算不能因重启延长，超时
迟到完成仍失败；创建结果不明不重发。取消失败明确 UNKNOWN，不声称资源已回收。

本次没有数据库、事件或 HTTP 接口迁移；验收报告升级到 v2，旧 v1 保留但不伪造恢复字段。
同步修正架构文档中已过期的“当前 Phase 16”描述，当前 P1/Phase99 状态以项目账本为准。
用户新增的每轮外部实时查证要求与 GitHub main 提交授权记录在
[范围决策](../product/productization-scope-decisions.md)。索引检索不能作为实时外部查询的
完成证据；这一接线能力及独立语义评分仍待实现。

最终完整 Python 回归 **2879 passed、268 skipped、7 deselected**（841.93 秒）通过，
测试期间 827 个源码/测试/配置文件摘要保持一致。最终验收专项 48 项通过（8.17 秒）；
此前包含契约的 84 项组合成绩单独记录，完整最终回归已经包含连续恢复修复。
客户端 Web 242、Desktop 17、IDE 12、TypeScript SDK 26，合计 **297 passed**；
类型、lint、构建、Ruff、1085 文件格式和 282 源文件 Mypy 通过，秘密扫描零发现。

初轮完整测试为修复连续恢复定位丢失而主动中断，不计作通过。当前增量无新数据库迁移，
本次未重跑真实 PostgreSQL 或厂商租户 UAT；历史数据库证据保持独立。离线完整验收
因未执行答案用例仍返回退出码 2。5 份真实企业原文/租户档案保留在本地忽略规则下，
不进入公开 GitHub 提交。完整账本见
[本次验证证据](../release/evidence/productization/20260914-p1-reconciliation-validation.json)。

用户最新[范围决策](../product/productization-scope-decisions.md)：云效代码严格只读，
允许不执行云效源码。真实构建缺口不再阻塞当前只读交付；保留 NOT_RUN，不计作通过。

## 已实现的本地增量

- 规范类问题与源码调查区分；“为什么”不再单凭代码词触发事故路由。
- 同主体已授权企业追问在路由前恢复原问题，并传入检索、调查、生成、复核。
- 追问复用结构化仓库/服务选择，仓库按当前 ACL 重查；撤权后不能自动换到其他仓库。
  字段来源、当前输入优先级及尚未覆盖项见[任务上下文合同](../product/p1-task-context-contract.md)。
- 工作台固定提交目录浏览、文本读取及返回父目录；每次导航受治理，过期响应与错误来源拒绝展示。
- 普通提问框支持从授权目录指定本轮仓库，通过原有 Turn 的 context_refs 提交，切换任务/空间隔离选择。
- 资料/输出/约束通过严格 task_context 接入普通任务，结构化继承、覆盖和清除已完成；所选资料按当前权限固定版本，初始和后续调查均限制在范围内。Web 支持选择和当前要求展示。
- 原生上传资料纳入发布、会话继承和历史读取的 Policy/ACL/版本检查；最近任务撤权及快照后撤权均停止追问，不回退到旧主题。
- 超预算证据保留可容纳的完整正文块、表格、代码和来源；显式标记遗漏，不输出截断 JSON。
- 离线评测为 27 个未执行答案用例逐项返回 NOT_RUN；新增整体 BLOCKED 与严格退出码，
  保留旧离线子集字段兼容。
- `acceptance freeze/run` 通过正式应用接口冻结部署声明/配置/资料并执行知识任务，
  记录最终答案、摘要、任务引用及逐项状态。当前独立评分器未配置，完成答案保留 NOT_RUN；
  签名和 Worker 版本证明未完成，阶段持续 BLOCKED。详见[操作说明](../operators/productization-acceptance.md)。

## 本次任务上下文增量

资料/输出接口初轮 145 项通过；扩大至来源发布、知识调查、多轮会话和错误处理后 201 项通过
（57.87 秒）；最后补齐快照后撤权和严格错误生产者登记后 27 项通过（50.92 秒）。Web 完整
242 项通过（6.07 秒），类型、lint 和构建通过。下表保留前一增量历史，完整新快照与 PostgreSQL
验证完成后另记本轮证据，不用旧全量结果代替本轮源码验证。

本次一次性 PostgreSQL 已完成真实升级及 256 passed、11 skipped、198 deselected
（188.35 秒），包括 6 项新增原生索引发布/历史权限回归；容器已清理，未连接生产数据库。
见[本次数据库证据](../release/evidence/productization/20260913-p1-context-postgres-invariants.json)。
原生检查初版提前返回曾使 10 项既有受管审计断言失败，已修复为继续执行受管检查，
未降低原断言；修复后的来源组合 165 项通过，并纳入后续扩大回归。

本次完整隔离 Python 回归 **2846 passed、268 skipped、7 deselected**（786.37 秒）通过，
运行期间源码快照未变。其后仅修正格式否定/引文边界并扩充正常五轮任务：183 项相关回归
（84.76 秒）与 38 项日常问答（6.92 秒）通过，没有重复宣称一次新的完整回归。输出形式只
从完整明确请求识别，“不要用表格”重置要求；资料引文中的相同词语不会改变任务设置。

本次 Web 242、Desktop 17、IDE 12、TypeScript SDK 26，合计 **297 passed**；Web 类型、lint、
构建及全仓 Ruff、1083 文件格式、四包 282 源文件类型检查均通过，秘密扫描零发现。
Desktop 初次回环监听被沙箱拒绝，获准临时本机监听后原 17 项通过；未修改测试断言。
完整快照、修正后快照、验证日志摘要及差异范围见
[本次任务上下文验证证据](../release/evidence/productization/20260913-p1-task-context-validation.json)。
GitHub main 已再次获取，仍与 HEAD 同为 de3dd18、0/0 分叉，工作区改动未提交或推送。

## 验证账本（前一增量）

| 检查 | 结果与边界 |
| --- | --- |
| 新增 P1 开发回归 | 118 passed，2.90 秒；含 100 条中文路由/追问组合和正常 HTTP 三轮问答，模型显式合成 |
| 既有与新增组合回归 | 195 passed，32.92 秒；该快照早于最后一个新增 HTTP 回归 |
| 严格类型检查 | 全四个 Python 包 279 源文件通过；后增只读 tree 模块类型检查通过 |
| 完整 Python 回归 | 最新完整隔离回归 2828 passed、262 skipped、7 deselected，761.43 秒，退出码 0；此前 2794 passed/1 failed 的环境示例遗漏已修复，不将旧失败轮写为通过；中途为补充上下文边界而主动中断的一轮也不计通过 |
| 配置与验收复验 | 53 passed，17.66 秒；环境示例与本地空值键对齐，未修改既有配置值，未填假提交或镜像 |
| 前端和客户端 | 目录增量 Web 231、Desktop 17、IDE 12、TypeScript SDK 26，共 286 passed；继续补齐提问框仓库选择后 Web 237 passed（9.98 秒），其他客户端未变，合计 292；Desktop 首次 2 项回环监听 EPERM，获准本机监听后原 17 项复验通过；Web 类型检查、lint 与构建通过 |
| 结构化追问组合 | 初轮 170 passed，11.01 秒；补入旧问题不作为新选择的边界及契约审查后 176 passed，47.23 秒；包含正常三轮入口选定仓库保持、当前选择优先和撤权后禁止替换仓库 |
| 验收驱动专项 | 25 passed，7.16 秒；含正式应用入口冻结/执行端到端，模型仍为显式合成适配器 |
| 一次性 PostgreSQL | 首次 249 passed、1 failed，为全局队列测试数据库未隔离；保持原断言，改用独立迁移数据库后 250 passed、11 skipped、198 deselected，153.95 秒；迁移与容器清理退出码均为 0 |
| 只读 tree 与旧云效回归 | 128 passed，30.32 秒；含固定提交、目录越界、LFS/子模块、截断、REST/Policy 及全部既有读取测试；外部响应为显式合成 |
| 接口和阶段契约组合 | 43 passed，40.74 秒；OpenAPI 已同步新增只读接口 |
| GitHub main | 再次 fetch，HEAD 与 origin/main 均为 de3dd181253c95c385112037f925b7784affb18a，提交分叉 0/0；git diff --check 通过 |
| 云效 | 未执行任何写入 |
| 数据库迁移 | 无新增 schema；无需结构迁移；临时数据库真实升级成功，未连接生产数据库；本机新增目录能力登记只获得 MASK，未达到登记要求的明确 ALLOW，事务已回滚，未新增运行绑定 |

上一增量的[本地验证证据](../release/evidence/productization/20260913-p1-local-increment-validation.json)
记录源码/测试/构建配置摘要和验证范围；Python 完整回归期间其源码未改变，期间补齐的
Web 选择入口已单独验证。工作区仍包含未提交改动，不冒充干净发布候选或生产验收。

## 未完成，不能通过阶段验收

- 首批24条原文检查后的保留用例已冻结（16可答、8资料不足），均为NOT_RUN；独立评分驱动及 ≥95% 真实路由验收未完成，开发组合回归不等于独立样本。
- 资料/输出/约束已完成普通接口和 Web 本地继承接线；真实模型遵循要求的独立评分、文件产物和真实钉钉全入口体验仍待验收。
- 验收驱动已能冻结并执行知识任务；独立语义评分、持久化 Worker 执行版本证明、无 Run 标识的创建对账与完整阶段硬门未完成。
- 已取得钉钉全部12文件清单及三仓库真实身份/固定提交/部分构建清单；完整tree、CI规范、不可变镜像和冻结测试入口仍缺。
- 真实模型完整问答验证、各来源/权限并发反向验收。

P1 不标记完成；P2—P5 和正式 Phase 99 门禁仍待各自真实证据。本报告不构成生产就绪声明。

## 真实来源补查

本地私有试点运行档案（`docs/product/p1-pilot-runtime-profiles.md`）记录129仓库完整授权目录、三仓库
固定commit、11次成功文件读取及31次HTTP403探查。所有远程读取经过既有Gateway/Policy；
403保留为阻塞，不能据此断言文件不存在。钉钉固定12文件（6可用、5部分、1失败），
没有移除失败项。DDBusiness.podspec实际依赖iOS/UIKit，用户曾授权补充受控macOS/Xcode验证环境；依最新范围决策，该环境属于可选验证，当前未实施也不计通过。

本地私有首批保留集（`evaluations/productization/p1-heldout-v1.json`）来自已完整读取的合同与财务
两篇制度；原文摘要与ETag核对，24条引文逐一匹配原文后冻结，尚未执行候选答案。
不得把这些引文作为答案提示传给被测Run，也不得把内部VERIFIED作为正确性评分。

两次早期直接pytest执行不作为通过证据：第一次受本机端口/进程沙箱限制而中断；
第二次加载开发.env模型白名单（15项模型相关失败），另有旧阶段检查器2项和错误转发
行号审查1项失败。后两类已修复并专项复验，未放宽任何错误枚举或审查范围。
最终完整回归使用scripts/test_local.py隔离dotenv、真实集成开关及插件注入。
