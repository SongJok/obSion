# M0 可信基线架构门

日期：2026-09-05

状态：进行中；不是生产晋级许可。

## 本次增量

- 将两份产品需求映射到 M0—M6 的独立验收出口。
- 增加显式本地测试入口，不修改生产 Settings、正式认证与 CI/live 测试语义。
- 本地测试不得继承应用环境或 pytest 注入配置；采用临时空工作目录，避免默认加载开发部署 `.env`。
- 数据库验证只使用本次创建的独立 PostgreSQL/pgvector 容器，不连接现有业务数据库。
- 阶段声明静态检查已接入 CLI、Makefile 和 CI，校验正式报告、对应架构门及显式登记的历史声明；只检查本地声明，不评估生产晋级授权。
- 语义兼容迁移加入独立 CI 数据库矩阵，本地验证升级数据保留与降级后结构重建；不将删除表后的重建描述为数据恢复。

## 保持的边界

单一 Python 控制面、PostgreSQL 事务事实源、统一 Harness、Capability Gateway、Policy Engine 和既有 API 均未变更。原 `make test` 与专用 live、迁移入口保持兼容。测试入口不是 OS 级网络沙箱；不对任意恶意测试代码提供隔离保证。

## 证据与未满足项

本次本地回归、数据库迁移及不变量结果见 [验证记录](../phases/productization-m0-validation.md)。发布候选契约明确 `promotion_eligible=false`；6 个操作方门禁仍为 PENDING。

M0 总门尚未关闭：当前是包含已有未提交修改的工作树，不是同一提交上的干净候选快照；历史完成报告和其他基线缺口仍需收敛。后续 M1 可信安装身份、Inbox、Outbox、群受众和真实厂商回执等改变运行语义，必须单独 ADR、迁移、测试及真实租户验收，不能借本门开放。

本次测试工具与文档增量无需数据库迁移，不引入新的运行时架构决策。
